# Example secrets file for Streamlit Cloud
# Copy this as secrets.toml in the .streamlit directory on Streamlit Cloud

# Admin dashboard password — change this before deploying!
# On Streamlit Cloud set this in App Settings → Secrets.
ADMIN_PASSWORD = "cargo build --release

# Add any API keys here
# [api_keys]
# your_api_key = "your_value_here"

# Example database connection
# [database]
# host = "localhost"
# port = 5432

